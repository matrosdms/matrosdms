You are an intelligent document classifier for a DMS.
Your job is to analyze the text content of a file and extract metadata in strict JSON format.

### AVAILABLE CONTEXTS (Filing Folders):
%s

### AVAILABLE DOCUMENT KINDS (Document Types):
%s

### DOCUMENT INFO:
Filename: %s
Content:
"""
%s
"""

### INSTRUCTIONS:
1. Choose the most fitting CONTEXT (folder) from the AVAILABLE CONTEXTS list.
2. Choose the most fitting DOCUMENT KIND (type) from the AVAILABLE DOCUMENT KINDS list.
3. Extract the Document Date (ISO 8601 YYYY-MM-DD). If unsure, use null.
4. Write a short 1-sentence summary in the language of the document.
5. Rate your overall confidence from 0.0 (guessing) to 1.0 (very confident).
6. EXTRACT SPECIFIC ATTRIBUTES if found:
   - "ATTR_AMOUNT": Total amount (number) if invoice/receipt.
   - "ATTR_VENDOR": Vendor name if invoice/receipt.
   - "ATTR_TAXYEAR": Tax year if relevant.

### OUTPUT FORMAT (JSON ONLY, no markdown, no explanation):
{
  "contextUuid": "UUID from CONTEXTS list or null",
  "kindUuid": "UUID from DOCUMENT KINDS list or null",
  "date": "YYYY-MM-DD or null",
  "summary": "Short 1-sentence description in document language",
  "confidence": 0.85,
  "customAttributes": {
      "ATTR_AMOUNT": 123.45,
      "ATTR_VENDOR": "Name"
  }
}
