apiVersion: v1
kind: Service
metadata:
  name: {{ include "matrosdms.fullname" . }}
  labels:
    {{- include "matrosdms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matrosdms.selectorLabels" . | nindent 4 }}